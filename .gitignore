# Archive photos — ~2GB, not needed for the app
archive/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor scratch files
*.swp
*~

# Node (test-harness scratch installs, e.g. jsdom)
node_modules/
package-lock.json

# Secrets — never commit
.secrets/
